- name: Restart Crowdsec
  ansible.builtin.systemd_service:
    name: crowdsec
    state: restarted
    daemon_reload: true
  become: true

- name: Restart NPM Container
  ansible.builtin.docker_container:
    name: edge-npm
    state: restart
  become: true
